{
  "name": "@my/monorepo",
  "description": "my mono-repo root package.json, used mainly to execute build scripts. This package is not published to npm.",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "pnpm -r build",
    "clean": "pnpm -r exec rm -rf dist",
    "lint": "pnpm -r lint",
    "dev": "pnpm --filter davo dev"
  }
}
